// Skill-drift fixture (error mode): the module no longer exports SAMPLE_SKILL,
// so there is nothing to extract. A renamed or removed export must fail the
// check loudly rather than silently check nothing.
// Never imported, compiled, or served.

export const SAMPLE_GUIDANCE = `# Fixture Skill — Sample Overlay

Done.
`;
