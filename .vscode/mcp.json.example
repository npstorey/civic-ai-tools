{
  // VS Code MCP configuration for GitHub Copilot
  // Auto-generated by scripts/setup.sh — or copy this file to .vscode/mcp.json
  // and replace the placeholder values with your API keys.
  //
  // For city workers behind a corporate proxy, see mcp.json.city-proxy-example instead.
  "servers": {
    "opengov": {
      "type": "stdio",
      "command": "node",
      "args": ["${workspaceFolder}/.mcp-servers/opengov-mcp-server/dist/index.js", "--stdio"],
      "env": {
        "DEFAULT_DOMAIN": "data.cityofnewyork.us",
        "SOCRATA_APP_TOKEN": "__SOCRATA_APP_TOKEN__",
        "CACHE_ENABLED": "true",
        "LOG_LEVEL": "info"
      }
    },
    "data-commons": {
      "type": "stdio",
      "command": "__DATACOMMONS_MCP_PATH__",
      "args": ["serve", "--skip-api-key-validation", "stdio"],
      "env": {
        "DC_API_KEY": "__DC_API_KEY__"
      }
    }
  }
}
